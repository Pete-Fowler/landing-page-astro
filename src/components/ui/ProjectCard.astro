---
interface Props {
    post: {
        title: string;
        date: string;
        author: string;
        slug: string;
    };
}

const { post } = Astro.props;
---

<a href={"/projects/" + post.slug} class="article-link">
    <article>
        <p class="date">
            {
                post.date && new Date(post.date).toLocaleDateString("en-US", {
                    dateStyle: "long",
                })
            }
        </p>
        <h4 class="title">{post.title}</h4>
        <p class="author">{post.author}</p>
    </article>
</a>
<style>
    .article-link {
        text-decoration: none;
        color: inherit;
        display: block;
    }

    article {
        margin-top: 1.5rem;
        box-shadow: 0 0 2px var(--text);
        border-radius: 0.25rem;
        border: 1px solid var(--accent-alt);
        padding: 1rem;
    }

    article:hover {
        box-shadow: 0 0 2px 2px var(--primary);
        transition: all 0.3s;
    }

    p.date {
        color: var(--accent-alt);
        font-size: 1rem;
        margin: 0;
        margin-bottom: 0.5rem;
    }

    p.author {
        color: var(--text-light);
        font-size: 1rem;
        margin: 0;
    }


</style>
